package flow;

public class ForLoop {
     public static void main(String[] args) {
          for(int i = 110 ; i >= 0 ; i -= 2){
               // "Count -= 2 means that the count will be substracted by two with each loop
               System.out.println("Count is : " + i);
          }
     }
}
